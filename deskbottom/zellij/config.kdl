// RootCellar Zellij configuration — the window manager of the deskbottom.
// Installed to /etc/cellar/zellij by modules/deskbottom.nix; discovered via
// the ZELLIJ_CONFIG_DIR environment variable.

default_layout "cellar"

themes {
    rootcellar {
        bg "#191622"
        fg "#F5F3F1"
        red "#D0879A"
        green "#6FBFAD"
        yellow "#D8C77A"
        blue "#5277C3"
        magenta "#A06BE0"
        orange "#C99A5B"
        cyan "#7EBAE5"
        black "#2A2438"
        white "#F5F3F1"
    }
}

theme "rootcellar"

// Restore tabs/panes across restarts.
session_serialization true
pane_frames true

copy_on_select true

ui {
    pane_frames {
        rounded_corners true
        hide_session_name true
    }
}

keybinds {
    normal {
        // Convenience only: everything else stays Zellij defaults.
        bind "Alt f" { ToggleFloatingPanes; }
        bind "Alt n" { NewPane "Right"; }
        bind "Alt d" { NewPane "Down"; }
        bind "Alt h" { MoveFocus "Left"; }
        bind "Alt l" { MoveFocus "Right"; }
        bind "Alt j" { MoveFocus "Down"; }
        bind "Alt k" { MoveFocus "Up"; }
    }
}
